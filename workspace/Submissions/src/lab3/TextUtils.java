package lab3;

public class TextUtils {
	/**
	 * Given a string (text),
	 * count the number of instances of each letter (`a`-`z` only) in an array.
	 * Use the ASCII code of each character as the index in an array.
	 * For example, in the string `hello world`,
	 * characters `w`, `h`, `e`, `r`, `d` each occur once,
	 * `o` occurs twice, and `l` occurs 3 times.
	 */
	public static int[] letterHistogram(String text) {
		return null;
	}
	/**
	 * Using `letterHistogram`,
	 * determine if two Strings are anagrams of each other.
	 * 
	 * Examples:
	 * `dormitory` is an anagram of `dirty room`
	 * `mars` is an anagram of `rams`
	 * 
	 * @return true if thing1 and thing2 are anagrams of each other, false otherwise.
	 */
	public static boolean isAnagram(String thing1, String thing2) {
		return true;
	}
}
